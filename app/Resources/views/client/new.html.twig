{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <h1 class="text-center text-uppercase page-header">Creer un nouveau client</h1>
    </div>

    <div style="height:80px;"></div>

    <div class="row">
        {{ form_start(form) }}
    
    
            <div class="col-lg-6 col-lg-offset-3">
                {{ form_label(form.nom) }}
                {{ form_widget(form.nom, {"attr": {"class": "form-control"}}) }}
        
                {{ form_label(form.prenom) }}
                {{ form_widget(form.prenom, {"attr": {"class": "form-control"}}) }}
        
                {{ form_label(form.telephone) }}
                {{ form_widget(form.telephone, {"attr": {"class": "form-control"}}) }}
                
                <div style="height:20px;"></div>
                
                <input class="btn btn-success btn-block" type="submit" value="Create" />
                
                <div style="height:20px;"></div>
                
            </div>
        {{ form_end(form) }}
    </div>

    <div class="row">
        <div class="col-lg-4 col-lg-offset-3">
            <ul class="list-unstyled">
                <li>
                    <a class="btn btn-default btn-block" href="{{ path('admin_client_index') }}">
                        <span class="glyphicon glyphicon-arrow-left"></span>
                        Retour a la liste des clients
                    </a>
                </li>
            </ul>
        </div>
    </div>
    <div style="height:20px;"></div>
    
{% endblock %}
