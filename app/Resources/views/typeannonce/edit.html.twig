{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <h1 class="text-center text-uppercase page-header">Editer le type d'annonce {{ typeAnnonce.id }} </h1>
    </div>

    <div style="height:80px;"></div>

    <div class="row">
        {{ form_start(edit_form) }}
    
    
            <div class="col-lg-6 col-lg-offset-3">
                {{ form_label(edit_form.nom) }}
                {{ form_widget(edit_form.nom, {"attr": {"class": "form-control"}}) }}
        
                <div style="height:20px;"></div>
                
                <input class="btn btn-success btn-block" type="submit" value="Creer" />
                
                <div style="height:20px;"></div>
                
            </div>
        {{ form_end(edit_form) }}
    </div>

    <div class="row">
        <div class="col-lg-4 col-lg-offset-3">
            <ul class="list-unstyled">
                <li>
                    <a class="btn btn-default btn-block" href="{{ path('admin_type-annonce_index') }}">
                        <span class="glyphicon glyphicon-arrow-left"></span>
                        Retour a la liste des type d'annonces
                    </a>
                </li>
            </ul>
        </div>
    </div>
    <div style="height:20px;"></div>
    
{% endblock %}